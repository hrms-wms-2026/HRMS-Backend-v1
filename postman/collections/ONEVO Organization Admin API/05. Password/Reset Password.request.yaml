$kind: http-request
name: Reset Password
method: POST
url: '{{base_url}}/api/v1/auth/reset-password'
order: 3000
headers:
  - key: Content-Type
    value: application/json
body:
  type: json
  content: |-
    {
      "token": "{{reset_token}}",
      "newPassword": "NewPassword123!"
    }
