$kind: http-request
name: Base Login
method: POST
url: '{{base_url}}/api/v1/auth/login'
order: 1000
headers:
  - key: Content-Type
    value: application/json
body:
  type: json
  content: |-
    {
      "email": "{{tenant_email}}",
      "password": "{{tenant_password}}"
    }
