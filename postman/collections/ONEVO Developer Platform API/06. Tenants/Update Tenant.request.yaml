$kind: http-request
name: Update Tenant
method: PATCH
url: '{{admin_base_url}}/admin/v1/tenants/{{tenant_id}}'
order: 5000
headers:
  - key: Content-Type
    value: application/json
  - key: X-CSRF-Token
    value: '{{admin_csrf_token}}'
body:
  type: json
  content: |-
    {
      "name": "Acme Pvt Ltd",
      "slug": "acme",
      "industryProfile": "technology"
    }
