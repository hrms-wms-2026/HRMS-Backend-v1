$kind: http-request
name: Verify Saved Service Key
method: POST
url: '{{admin_base_url}}/admin/v1/system-config/service-keys/{{service_key}}/verify'
order: 6000
headers:
  - key: X-CSRF-Token
    value: '{{admin_csrf_token}}'
