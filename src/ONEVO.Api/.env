# Local development environment defaults.
# DotEnvLoader only applies these if the variable is NOT already set in the process,
# so VS launchSettings / shell exports always take precedence.
ASPNETCORE_ENVIRONMENT=Development
ASPNETCORE__Swagger__Enabled=true
