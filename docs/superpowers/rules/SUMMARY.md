# rules/ — Summary

**Purpose:** Standing process rules for how `docs/superpowers/` is maintained across sessions.

**Last updated:** 2026-08-03

## Files

- `PROCESS_RULES.md` — the 7 standing rules: plan-before-work, keep `project_ core/` docs in sync with decisions, per-folder `SUMMARY.md` maintenance, workflow reports outrank generic architecture claims when they conflict, the edit boundary (only `project_ core/` gets content edits; other folders are new-file-only), every finished API endpoint gets a plain-Markdown doc under `docs/postman-request/` (rule 6 — supersedes an earlier same-day `postman/collections/` yaml rule, now dropped as unnecessary), and file-creation/organization rules for `plans/` live in `FILE_CREATION_RULES.md` (rule 7, added 2026-08-06).
- `FILE_CREATION_RULES.md` — governs `docs/superpowers/plans/` and (since 2026-08-06) `docs/superpowers/specs/`: the `finished/`/`next/`/`pin/` folder layout (no author-based subfolder — a `kajaa/` personal folder was dissolved into `finished/` the same day), the pending/finished status field tracked in each folder's `SUMMARY.md`, the ~300-line-target-but-split-by-task rule for new plans/designs (Part 1/Part 2 sub-files, each independently self-contained), and that this splitting applies to new file creation only — existing files are migrated by moving + status-tagging, never retroactively split.

## Open items

None.
